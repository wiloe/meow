{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug (Clang/LLVM)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/emu6502.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "externalTerminal",
            "preLaunchTask": "CMake Build"
        }
    ]
}